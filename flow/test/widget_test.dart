// Smoke test placeholder — replace with real widget tests once the app matures.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
